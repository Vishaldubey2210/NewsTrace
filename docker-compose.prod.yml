version: '3.8'

services:
  newstrace-app:
    image: newstrace:latest
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - FLASK_ENV=production
      - FLASK_PORT=5000
    ports:
      - "5000:5000"
